apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-test-deployment
  labels:
    app: hello-app-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hello-app-test
  template:
    metadata:
      labels:
        app: hello-app-test
    spec:
      containers:
        - name: hello-app-test
          image: gcr.io/google-samples/hello-app:2.0
          
          ports:
            - containerPort: 8080
